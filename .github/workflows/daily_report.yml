name: Daily Semiconductor News Update

on:
  schedule:
    - cron: '29 23 * * *'  # 🌍 UTC 21:00 = 한국 시간(KST) 다음날 08:29
  workflow_dispatch:      # 수동 실행 버튼 활성화

jobs:
  build:             # (스페이스 2칸)
    runs-on: self-hosted
    env:             # (스페이스 4칸) build 바로 아래에 위치
      PYTHONIOENCODING: utf-8
    permissions:     # (스페이스 4칸)
      contents: write

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      # [라이브러리 설치] main.py 구동에 필요한 모든 것
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install google-genai elevenlabs feedparser python-dateutil googlenewsdecoder requests

      - name: Run script
        env: # 🔑 GitHub Secrets와 연결 (Settings에서 미리 등록 필수!)
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          ELEVENLABS_API_KEY: ${{ secrets.ELEVENLABS_API_KEY }}
          KAKAO_REST_API_KEY: ${{ secrets.KAKAO_REST_API_KEY }}
          KAKAO_CLIENT_SECRET: ${{ secrets.KAKAO_CLIENT_SECRET }}
          KAKAO_REFRESH_TOKEN: ${{ secrets.KAKAO_REFRESH_TOKEN }}
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
        run: python main.py

      - name: Commit and Push changes
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          git add .
          # TZ='Asia/Seoul'을 넣어야 커밋 메시지 날짜가 한국 시간으로 찍힙니다.
          export TZ='Asia/Seoul'
          git commit -m "🚀 News Update: $(date +'%Y-%m-%d')" || exit 0
          git push
