name: Daily Semiconductor News Update

on:
  schedule:
    - cron: '13 22 * * *'  # 🌍 한국 시간(KST) 다음날 07:13
  workflow_dispatch:      # 수동 실행 버튼 활성화

jobs:
  build:             # (스페이스 2칸)
    runs-on: self-hosted
    env:             # (스페이스 4칸) build 바로 아래에 위치
      PYTHONIOENCODING: utf-8
    permissions:     # (스페이스 4칸)
      contents: write

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      # [라이브러리 설치] main.py 구동에 필요한 모든 것
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install google-genai elevenlabs feedparser python-dateutil googlenewsdecoder requests

      - name: Run script
        env: # 🔑 GitHub Secrets와 연결 (Settings에서 미리 등록 필수!)
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          ELEVENLABS_API_KEY: ${{ secrets.ELEVENLABS_API_KEY }}
          KAKAO_REST_API_KEY: ${{ secrets.KAKAO_REST_API_KEY }}
          KAKAO_CLIENT_SECRET: ${{ secrets.KAKAO_CLIENT_SECRET }}
          KAKAO_REFRESH_TOKEN: ${{ secrets.KAKAO_REFRESH_TOKEN }}
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
        run: python main.py

      - name: Commit and Push changes
        shell: powershell
        run: |
          # 0. PowerShell 한글 출력 인코딩 강제 설정 (UTF-8)
          [Console]::OutputEncoding = [System.Text.Encoding]::UTF8
          
          # 1. 보안 예외 처리
          git config --global --add safe.directory C:/actions-runner/actions-runner/_work/semi-daily-news/semi-daily-news
          
          # 2. 사용자 설정
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          
          # 3. 변경 사항 확인 (매우 중요)
          # git status --porcelain은 변경 사항이 없으면 아무것도 출력하지 않습니다.
          $changes = git status --porcelain
          if ($null -eq $changes -or $changes -eq "") {
              Write-Host "✅ 업데이트된 뉴스가 없거나 변경 사항이 없습니다. 공정을 종료합니다."
              exit 0
          }
          
          # 4. 변경 사항 추가 및 커밋
          git add .
          $currentDate = Get-Date -Format "yyyy-MM-dd"
          git commit -m "🚀 News Update: $currentDate"
          
          # 5. 푸시
          Write-Host "📡 새로운 리포트를 서버로 전송합니다..."
          git push origin main
